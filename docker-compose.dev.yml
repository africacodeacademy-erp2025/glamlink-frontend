# This file automatically overrides docker-compose.yml in development
# Just run: docker-compose up --build
# Docker Compose automatically applies this override file

services:
  glamlink-app:
    container_name: glamlink-dev
    build:
      dockerfile: Dockerfile.dev
      args:
        NODE_ENV: development
        NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL}

    # Load development environment variables
    env_file:
      - .env

    # Override environment
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_BACKEND_URL=${NEXT_PUBLIC_BACKEND_URL}
      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
      - CLOUDINARY_FOLDER=${CLOUDINARY_FOLDER:-glamlink_profiles}

    # Enable hot reload with volume mounts
    volumes:
      - ./src:/app/src
      - ./public:/app/public

    restart: always
